output: count,method,uri,2xx,3xx,4xx,5xx,max,avg,sum,p90,p99,sum_body,avg_body
sort: sum
reverse: true
limit: 65536
matching_groups:
  # >>> contest values >>>
  # Normalize the contest's dynamic path segments here, most specific first.
  # <<< contest values <<<

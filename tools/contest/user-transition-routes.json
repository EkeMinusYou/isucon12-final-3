{
  "_comment": "Placeholder. Set cookie_field to the access log column that identifies a session, api_prefix to the API root, and replace routes with the contest's own paths: dynamic IDs normalized, most specific pattern first.",
  "cookie_field": "session_id",
  "api_prefix": "/",
  "routes": [
    {
      "name": "/initialize",
      "pattern": "^/initialize$"
    }
  ]
}

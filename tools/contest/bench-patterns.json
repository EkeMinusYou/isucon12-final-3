{
  "_comment": "Contest-specific benchmark log format. Replace the expressions for each contest; the generic core and the emitted markers stay unchanged. score needs exactly one capture group; leave an entry empty when the benchmarker has no such line.",
  "score": "score:\\s*(\\d+)\\s*$",
  "pass": "^\\s*pass\\s*$",
  "fail": "^\\s*fail\\s*$"
}

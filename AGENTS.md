# AGENTS.md

## 目的と改善判断の原則

公式ルールと正当性要件を満たし、最終スコア（目標100万点）を最大化する。

### Evidenceに基づいて判断する

- 時間・処理量・待ち・capacityを同じ単位・母数で比較し、同一RUN・時間窓のEvidenceで判断する。割合だけで律速を決めない
- 欠損成果物を0扱いせず、`run.json`のartifact statusと`missing`を確認する。失敗RUNもfinalizeし、成果物全体は`task artifacts-run`で確認する
- 数値集計・時系列結合・RUN横断解析は既存DuckDB（`task q -- "SQL"`）を優先し、`tools/analysis/sources.yaml`・`tools/analysis/schema/`・既存ビューを使う。未対応処理や単純確認は別手段可。DuckDB利用のために計測基盤を変更しない

### 現行構成からの追加改善を考える

- 現行実装に残る仕事・待ち・転送・資源消費と発生条件をEvidenceで確認し、追加・補完・置換による増減、正当性、得点寄与を現行と比較する。効果量が未確定でも、根拠・仮説・評価条件があれば次へ進める

## ISUCONでの変更方針

- 競技時間内の改善と最終スコアを優先する。一般的な本番サービスの無停止移行、旧実装との互換運用、既存データの保持を暗黙の必須条件にしない
- 競技時間には制限があるため、テストと確認は正当性・安全性・変更判断に必要な範囲へ最小限に絞り、網羅的な検証や将来の問題に備えた確認へ時間を広げない
- ISUCONのベンチマーカーは実行回数に限りがあり、段階的に小さな変更を繰り返して検証することは難しい。実行前にEvidenceと影響範囲を整理し、効果が見込める変更をまとめて選定して、限られたベンチ機会を比較評価に使う
- `POST /initialize`は負荷走行・得点計測の対象外なので初期化時間の短縮は優先しない。正常完了の範囲で初期化時の処理量増加を許容し、負荷時の性能と得点を優先するが、公式の完了時間制限、初期状態の再現性、正当性、再起動後の永続性を維持し、競技DBを再作成して負荷時の書込みを追試で取得できるようにする
- 依頼された改善に必要な破壊的変更は許可済みとする。競技DB・テーブルの破棄と再作成、スキーマや配置の変更、不要なコード・設定・ファイルの削除を許容し、破壊的であることだけを理由に再確認しない。対象は依頼の範囲内とし、ユーザーが明示したデータ保持条件は守る
- ロールバックの計画・専用実装・事前検証は不要。修正が必要になった時点で、コードと変更履歴を使って対応する。将来の切り戻しだけを目的に、旧コードの併存、バックアップ、切替フラグ、逆移行・復旧スクリプトを追加しない。不要になったコードとファイルは削除する
- まず既存のTaskfile・設定・公式初期化処理で目的を満たす。DB配置変更は接続先設定と`task db-recreate`による再作成を使う。独自スクリプトや運用手順は理解・保守の負担になるため、依頼の範囲内の最小変更を優先する

## 禁止事項と作業上の制約

### 調査・ベンチ実行

- 当該競技やベンチマーカーをインターネットで調べず、ベンチマーカーを実行しない。必要な場合はユーザーに依頼し、setup依頼を許可とみなさない

### 編集・デプロイ

- 全操作の入口と構成の正本は`Taskfile.yml`。冒頭の`APP_NAME`、`SERVICE`、`DB_NAME`、`ALL_HOSTS`、`IP`、`*_HOSTS`を競技ごとに更新する
- 競技固有・解法固有のタスクは`Taskfile.contest.yml`へ置き、`Taskfile.yml`は汎用のまま保つ。両ファイルは同一の名前空間なのでタスク名を重複させない。汎用側から呼ぶ場合はhookタスク名で参照する
- 競技固有の設定・ツールは`tools/contest/`へ置く。`tools/`の他のディレクトリは、そこを参照する側であっても競技非依存に保つ。分けられない値は該当行へ`# contest:`コメントを残す。詳細は[tools/contest/README.md](tools/contest/README.md)
- 競技ごとに書き換える`Taskfile.yml`のvarsは`# >>> contest values >>>`区画に閉じ込める。templateへ戻す差分は`task template-diff`で抽出する。詳細は[tools/template/README.md](tools/template/README.md)
- サーバー上で直接編集せず、`task setup-*`で取得し、ローカル編集後に`task deploy-*`で反映する
- アプリはGo実装を編集・deploy対象とする。Node実装（`webapp/node/`）を含む他言語の参考実装は参照専用とし、編集せず正規deploy経路へ混ぜない
- Goアプリとcollectorは、実ホストで確認した`TARGET_OS` / `TARGET_ARCH`へローカルでクロスコンパイルする
- `nginx/conf.d/upstream.conf`は`task gen`の生成物なので手編集しない
- `task deploy-*`はreload/restart、`task apply-roles`はサービスのenable/disableを伴う。実行前に対象ホストと影響を確認する
